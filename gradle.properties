org.gradle.daemon=true
org.gradle.parallel=true
# Turn the following on once you finished changing your settings/build:
# org.gradle.configuration-cache=true
# org.gradle.caching=true

# Hytale location for Assets.zip resolution (Flatpak install):
hytale.home_path=/home/keroppi/.var/app/com.hypixel.HytaleLauncher/data/Hytale

# Single source of truth for both Natural20.jar and Natural20-Patches.jar artifact versions.
# tools/nat20-patches/ is a separate root project that reads this file via a relative path
# in its build.gradle.kts. Bump here to bump both jars together.
version=1.1.0